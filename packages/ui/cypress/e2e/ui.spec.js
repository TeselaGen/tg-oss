describe("zoomCircularView 1.spec", function () {
  it(`should work 1`, function () {
    cy.visit("");
    //a bunch of things should exist/not exist
    cy.get("div:contains(Zoink)")

  });
  it(`should work 2`, function () {
    cy.visit("");
    //a bunch of things should exist/not exist
    cy.get("div:contains(Zoink)")
  });
  it(`should work 3`, function () {
    cy.visit("");
    //a bunch of things should exist/not exist
    cy.get("div:contains(Zoink)")
  });
  it(`should work 4`, function () {
    cy.visit("");



    //a bunch of things should exist/not exist
    cy.get("div:contains(Zoink)")

  });
  it(`should work 5`, function () {
    cy.visit("");
    //a bunch of things should exist/not exist
    cy.get("div:contains(Zoink)")

  });
  it(`should work 6`, function () {
    cy.visit("");
    //a bunch of things should exist/not exist
    cy.get("div:contains(Zoink)")

  });
  it(`should work 7`, function () {
    cy.visit("");
    //a bunch of things should exist/not exist
    cy.get("div:contains(Zoink)")

  });
  it(`should work 8`, function () {
    cy.visit("");
    //a bunch of things should exist/not exist
    cy.get("div:contains(Zoink)")

  });
  });
describe("zoomCircularView 2.spec", function () {
  it(`should work 1`, function () {
    cy.visit("");
    //a bunch of things should exist/not exist
    cy.get("div:contains(Zoink)")

  });
  it(`should work 2`, function () {
    cy.visit("");
    //a bunch of things should exist/not exist
    cy.get("div:contains(Zoink)")

  });
  it(`should work 3`, function () {
    cy.visit("");
    //a bunch of things should exist/not exist
    cy.get("div:contains(Zoink)")

  });
  it(`should work 4`, function () {
    cy.visit("");
    //a bunch of things should exist/not exist
    cy.get("div:contains(Zoink)")

  });
  it(`should work 5`, function () {
    cy.visit("");
    //a bunch of things should exist/not exist
    cy.get("div:contains(Zoink)")

  });
  it(`should work 6`, function () {
    cy.visit("");
    //a bunch of things should exist/not exist
    cy.get("div:contains(Zoink)")

  });
  it(`should work 7`, function () {
    cy.visit("");
    //a bunch of things should exist/not exist
    cy.get("div:contains(Zoink)")

  });
  it(`should work 8`, function () {
    cy.visit("");
    //a bunch of things should exist/not exist
    cy.get("div:contains(Zoink)")

  });
  });
describe("zoomCircularView 3.spec", function () {
  it(`should work 1`, function () {
    cy.visit("");
    //a bunch of things should exist/not exist
    cy.get("div:contains(Zoink)")

  });
  it(`should work 2`, function () {
    cy.visit("");
    //a bunch of things should exist/not exist
    cy.get("div:contains(Zoink)")

  });
  it(`should work 3`, function () {
    cy.visit("");
    //a bunch of things should exist/not exist
    cy.get("div:contains(Zoink)")

  });
  it(`should work 4`, function () {
    cy.visit("");
    //a bunch of things should exist/not exist
    cy.get("div:contains(Zoink)")

  });
  it(`should work 5`, function () {
    cy.visit("");
    //a bunch of things should exist/not exist
    cy.get("div:contains(Zoink)")

  });
  it(`should work 6`, function () {
    cy.visit("");
    //a bunch of things should exist/not exist
    cy.get("div:contains(Zoink)")

  });
  it(`should work 7`, function () {
    cy.visit("");
    //a bunch of things should exist/not exist
    cy.get("div:contains(Zoink)")

  });
  it(`should work 8`, function () {
    cy.visit("");
    //a bunch of things should exist/not exist
    cy.get("div:contains(Zoink)")

  });
  });
